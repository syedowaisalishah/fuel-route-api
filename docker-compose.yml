services:
  web:
    build: .
    command: sh -c "python manage.py migrate && python manage.py import_fuel_prices data/sample_fuel_prices.csv && python manage.py runserver 0.0.0.0:8000"
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
